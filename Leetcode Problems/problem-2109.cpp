//1st Approach
            // str1 = s.substr(0,spaces[i]+i);
            // str2 = s.substr(spaces[i]+i,s.length()-(spaces[i]+i));
            // str1.append(" ");
            // s = str1 + str2;
            //2nd Approach
            // s.insert(spaces[i]+i," ");
            //3rd approach